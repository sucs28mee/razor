main := fn() {
    print("Hello world!");
    x := 92.9;
}

LinkedList := struct(value: int, next: &LinkedList?) {
    pub push := (self, value: int) {
        node := self;
        loop next ?= node.next {
            node = next;
        }
    
        node.next = &LinkedList(value, ?);
    }
}